# ЗАДАНИЕ 2

cort_1 = (1, 2, 3, 4, 'bioinf', 1)

# Так как кортежи неизменяемы, то добавлять и удалять из них элементы нельзя
print(cort_1.count(1))
print(len(cort_1))

# Сложение кортежей
cort_1 += cort_1
print(cort_1)
